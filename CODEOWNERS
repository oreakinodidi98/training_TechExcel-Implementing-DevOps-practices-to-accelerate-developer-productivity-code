# This is a comment.
# Each line is a file pattern followed by one or more owners.

# These owners will be the default owners for everything in the repo.
* @oreakinodidi98 

# Order is important; the last matching pattern takes the most precedence.
# When someone opens a pull request that only modifies JS files, only @js-owner and not the global
# owner(s) will be requested for a review.
*.js @js-owner

# You can also use email addresses if you prefer. They'll be used to look up users just like the @username form.
/docs/* oreakinodidi@gmail.com

# In this example, @doctocat owns any files in the build/logs directory at the root of the repository
# and any of its subdirectories.
/build/logs/ @oreakinodidi98

# The `InfrastructureAsCode/*` pattern will match files like
# `InfrastructureAsCode/getting-started.md` but not further nested files like
# `InfrastructureAsCode/build-app/troubleshooting.md`.
/InfrastructureAsCode/* @oreakinodidi98

# In this example, @octocat owns any file in an apps directory anywhere in your repository.
/Application/ @oreakinodidi98